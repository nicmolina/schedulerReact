/** Logout component
 * @const Logout
 * component that will be displayed after logout, its not being used on this version of this project
 */

import React from 'react';

const Logout = () => {
    return (
        <div>
            <h1>Thanks for using WorkSpace Hours+</h1>
        </div>
    )
}

export default Logout;